# Trust all connections for local development (Windows host -> Docker Postgres)
# For "local", format is: TYPE  DATABASE  USER  METHOD (no ADDRESS)
# For "host", format is: TYPE  DATABASE  USER  ADDRESS    METHOD
local   all             all            trust
host   all             all   0.0.0.0/0 trust
host   all             all   ::0/0     trust
